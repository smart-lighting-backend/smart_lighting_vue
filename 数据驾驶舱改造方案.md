# 数据驾驶舱改造方案

## 一、目标

将当前 `Dashboard.vue` 从**单列滚动式页面**改造为**全屏数据驾驶舱**，达到商用项目招标展示标准。

---

## 二、布局设计

```
┌──────────────────────────────────────────────────────────────┐
│                       顶部标题栏 (60px)                       │
├──────────┬────────────────────────────────────┬──────────────┤
│          │                                    │              │
│  左上    │                                    │  右上        │
│  统计    │        中间核心区域                 │  实时        │
│  面板    │    (3D 场景 / 地图 切换)            │  数据        │
│          │                                    │  面板        │
│  左下    │                                    │  右下        │
│  图表面板 │                                    │ AI边缘决策   │
│          │                                    │ (占位)       │
├──────────┴────────────────────────────────────┴──────────────┤
│                    底部滚动信息条 (40px)                       │
└──────────────────────────────────────────────────────────────┘
```

**关键原则：一屏展示全部信息，不需要滚动。** 用 `100vh` 填满视口，`overflow: hidden`。

### 区域划分

| 区域 | 位置 | 宽度比 | 内容 |
|---|---|---|---|
| 顶部标题栏 | top | 100% | 系统标题、时间、天气、在线设备数、当前能耗 |
| 左上统计面板 | left-top | ~22% | 设备总数/在线率/节能率/今日能耗 (数字滚动) |
| 左下趋势面板 | left-bottom | ~22% | 能耗实时曲线 + 分区能耗柱状图 |
| 中间核心区 | center | ~56% | **3D 数字孪生场景**（主）或 设备分布地图（切换） |
| 右上实时面板 | right-top | ~22% | 区域在线率环形图 + 状态分布 + 最新上线设备滚动列表 |
| 右下AI面板 | right-bottom | ~22% | **AI 边缘决策（占位）** — 标题+留空，后续开发 |
| 底部信息条 | bottom | 100% | 滚动消息：最新事件、系统通知 |

---

## 三、各区域详细设计

### 3.1 顶部标题栏

```
┌──────────────────────────────────────────────────────────────┐
│  ◆ 智慧路灯数字孪生驾驶舱    2026-07-09 14:32:05   设备 1,248 │
│                             晴天 32°C              在线 95.0% │
└──────────────────────────────────────────────────────────────┘
```

- 左侧：项目Logo + 标题
- 中间：装饰粒子光线
- 右侧：日期时间（每秒刷新）、天气（静态或接入API）、在线率
- 底部发光线（渐变呼吸动画）

### 3.2 左上 — 核心统计面板

4 个统计卡片，**数字滚动动画**（从 0 跳到目标值）：

```
┌─────────────────────┐
│ ◆ 核心指标          │
│                     │
│ ┌───────────────┐   │
│ │ 设备总数      │   │
│ │ 1,248 台     │   │  ← 数字从 0 跳动到 1248
│ │ 较昨日 +12   │   │
│ └───────────────┘   │
│ ┌───────────────┐   │
│ │ 在线率        │   │
│ │ ◎ 95.0%     │   │  ← 小型环形进度条
│ └───────────────┘   │
│ ┌───────────────┐   │
│ │ 节能率        │   │
│ │ 32.5%        │   │
│ │ ████████░░   │   │
│ └───────────────┘   │
│ ┌───────────────┐   │
│ │ 今日能耗      │   │
│ │ 4,286.5 kWh  │   │
│ └───────────────┘   │
└─────────────────────┘
```

- 每个卡片有科技风边框（发光边角装饰）
- 数字变化时带 `transition` 或 `requestAnimationFrame` 动画
- 在线率配小型 ECharts 环形图

### 3.3 左下 — 能耗趋势面板

```
┌──────────────────────────┐
│ ◆ 能耗趋势               │
│                          │
│  [今日 vs 上周 折线图]    │
│  (ECharts, 带渐变填充)    │
│                          │
│ ──────────────────────── │
│ ◆ 分区能耗排名           │
│                          │
│  高新区   ██████████ 386  │
│  市中心   ████████   312  │
│  创业园区 ██████     201  │
│  工业园   █████      187  │
│  学院路段 ███        100  │
│                          │
└──────────────────────────┘
```

- 上方：能耗折线图（已有，调整尺寸填入）
- 下方：横向柱状排名图（ECharts bar）

### 3.4 中间 — 3D 数字孪生核心区

**这是整个驾驶舱的视觉焦点。**

```
┌──────────────────────────────────────┐
│  [3D场景]                    [切换]  │  ← 顶部工具栏
│                                      │
│                                      │
│         城市路灯 3D 场景               │
│    - 地面网格/道路纹理                │
│    - 路灯 3D 模型排列                 │
│    - 在线=青色光 离线=灰色 告警=红色  │
│    - 摄像机缓慢自动旋转               │
│    - 点击路灯弹出详情浮层             │
│                                      │
│                          ┌──────┐    │
│                          │图例  │    │
│                          └──────┘    │
└──────────────────────────────────────┘
```

**实现方式：**

- 使用现有的 `createScene` / `useThreeScene`
- 用 Three.js 构建简单的路灯模型（CylinderGeometry + SphereGeometry + PointLight）
- 地面：GridHelper + 道路纹理平面
- 设备数据映射到 3D 位置（用已有坐标数据，或随机排列展示效果）
- 在线设备：青色点光源（带 bloom 辉光）
- 离线设备：灰色无光
- 告警设备：红色脉冲光（scale 动画）
- OrbitControls 设置 `autoRotate: true, autoRotateSpeed: 0.3`
- 顶部放"3D视图/地图视图"切换按钮，切到地图时显示现有的 `DeviceMap`

**不需要引入后处理（post-processing）**，简单光源 + 颜色即可达到不错的视觉效果。如果后续想要 glow/bloom 效果，可以加 `EffectComposer`。

### 3.5 右上 — 实时设备状态 + 告警

```
┌─────────────────────────┐
│ ◆ 设备实时状态          │
│                         │
│  [环形图: 在线率 95%]   │
│                         │
│  在线    1,186  ● 青色  │
│  离线       47  ● 灰色  │
│  告警       12  ● 红色  │
│  停用        3  ● 暗色  │
│                         │
│ ──────────────────────  │
│ ◆ 最新告警              │
│  ⚡ 设备离线  SL-0012   │
│    创业园区  14:32      │
│  ⚡ 亮灯异常  SL-0089   │
│    高新区    14:28      │
│                         │
│  [告警中心 →]           │
└─────────────────────────┘
```

- 上半部：ECharts 环形图（在线率可视化）+ 状态分布文字列表
- 下半部：最新 3 条告警，带类型图标和时间
- 底部分流按钮跳转到告警中心
- 配合 MQTT 实时更新

### 3.6 右下 — AI 边缘决策（占位面板）

```
┌─────────────────────────┐
│ ◆ AI边缘决策            │
│                         │
│                         │
│      🤖                 │
│   边缘AI决策             │
│   功能开发中...          │
│                         │
│                         │
│                         │
└─────────────────────────┘
```

- 保留完整的面板框架和四角发光装饰，与其它面板风格一致
- 中间显示占位图标 + "AI边缘决策" + "功能开发中..." 灰色提示文字
- 后续可替换为真实的边缘决策日志列表和触发按钮
- 告警信息移到右上实时面板的列表中合并展示

### 3.7 底部滚动信息条

```
┌──────────────────────────────────────────────────────────────┐
│  ▶ 系统通知：2026年度第二季度节能报告已生成 | 最新事件：SL-0238设备上线 | ...  │
└──────────────────────────────────────────────────────────────┘
```

- CSS `marquee` 或 `requestAnimationFrame` 实现无缝滚动
- 显示系统通知、最新事件、设备状态变更

---

## 四、视觉风格规范

| 元素 | 样式 |
|---|---|
| 背景色 | `#060e1f` 深蓝黑底 |
| 面板背景 | `rgba(8, 24, 52, 0.85)` 半透明 + `backdrop-filter: blur(12px)` |
| 主强调色 | `#4dd0e1` 青色 |
| 辅助色 | `#00c8b4` 青绿 (在线)、`#ef5350` 红 (告警)、`#ffa726` 橙 (异常) |
| 面板边框 | `1px solid rgba(77, 208, 225, 0.15)` + 四角装饰发光 L 形 |
| 标题样式 | 左侧青色竖线 + 文字，如 `border-left: 3px solid #4dd0e1` |
| 字体 | 数字用 `'DIN', 'Orbitron', monospace` 风格（可用 `font-family: 'Courier New', monospace` 近似） |

### 面板四角装饰（CSS 伪元素实现）

```
┌──────────────────────┐
│                      │  每个角一个 L 形发光短线
│      内容区域        │  ::before + ::after + border
│                      │  颜色: #4dd0e1, 发光: box-shadow
└──────────────────────┘
```

这是商用驾驶舱最标志性的视觉元素，纯 CSS 实现，不需要图片。

---

## 五、实现计划（分步）

### 第一步：布局骨架 (预计 ~200 行)
- 新建 `Dashboard.vue` 的 HTML/CSS 全屏 grid 布局
- 6 个面板区域的 div 框架
- 顶部标题栏 + 底部滚动条
- 四角发光边框装饰

### 第二步：3D 数字孪生场景 (预计 ~250 行)
- 在中间面板挂载 Three.js 场景
- 构建简单的路灯 3D 模型（杆+灯罩+光源）
- 根据在线/离线/告警状态切换光源颜色
- 摄像机自动旋转
- 点击交互（高亮 + 弹出信息面板）

### 第三步：统计面板 + 数字动画 (预计 ~150 行)
- 数字滚动动画 composable（`useCountUp`）
- 4 个统计卡片的数据绑定

### 第四步：图表面板 (预计 ~200 行)
- 环形图（在线率）
- 实时曲线图（能耗）
- 横向柱状图（分区排名）
- 迷你告警趋势图
- 缩放适配

### 第五步：实时数据联动 (预计 ~100 行)
- MQTT 订阅驱动面板更新
- 3D 场景中设备状态实时切换
- 右上最新上线设备列表滚动更新
- 底部信息条内容更新

### 第六步：3D/地图视图切换 (预计 ~50 行)
- 中间区域切换按钮
- 地图视图复用现有 `DeviceMap` 组件

---

## 六、不引入新依赖

仅使用项目中已有的库：

- **Vue 3** — 框架
- **Three.js** (`three`) — 3D 场景
- **ECharts** (`echarts`) — 图表
- **Element Plus** — UI 组件（少量使用 `el-select` 等）
- **MQTT** (`mqtt`) — 实时数据
- **高德地图** — 设备地图（已有 DeviceMap 组件）

数字动画、发光边框、粒子效果均用纯 CSS 或 Canvas/Three.js 实现，不需要额外 npm 包。

---

## 七、效果预期

改造后，打开 `/dashboard` 将看到一个完整的**全屏数据驾驶舱**：

- 中间是一个缓慢旋转的 3D 城市路灯场景，青色的光点代表在线设备
- 两侧面板展示实时统计、图表、告警
- 数字跳动、光源呼吸、边框发光、底部信息滚动
- 可以切换到地图视图查看设备地理分布
- 适合投屏到展厅大屏、招标演示

---

## 八、风险与取舍

| 风险 | 应对 |
|---|---|
| 3D 性能 | 路灯模型采用最简单的几何体（圆柱+球体），不加后处理，保持 60fps |
| 数据量 | 设备点位超过 1000 时 3D 场景会卡 → 只展示在线/告警设备，或分区域加载 |
| 地图 3D 切换 | 两个视图都挂载可能内存高 → 切换时销毁/重建，或用 `v-if` |
| 响应式 | 驾驶舱设计为 1920×1080 基准，ScreenScale 等比缩放已处理其他分辨率 |
| 浏览器兼容 | 驾驶舱类页面通常只在展厅大屏 Chrome 上运行，不用考虑移动端/IE |
---

## 九、文件变更清单

| 文件 | 操作 | 说明 |
|---|---|---|
| `src/views/Dashboard.vue` | **重写** | 主驾驶舱页面，约 800-1000 行 |
| `src/composables/useCountUp.js` | **新建** | 数字滚动动画 composable |
| `src/composables/useThreeScene.js` | 可能需要微调 | 支持更多初始化参数 |
| `src/components/Dashboard3D.vue` | **新建** (可选) | 3D 场景子组件，保持 Dashboard 简洁 |

**总新增代码量：约 1000-1500 行（含 HTML/CSS/JS）**

---

## 十、时间预估

- 布局骨架 + 视觉装饰：1-2 小时
- 3D 场景：2-3 小时
- 图表 + 数字动画：2-3 小时
- 数据联动 + 调试：1-2 小时

**总计：约 6-10 小时**
